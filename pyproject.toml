[project]
name = "bpe-vitaldb"
version = "0.1.0"
description = "Blood pressure estimation from PPG using VitalDB and PyTorch"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.10.9",
    "tqdm>=4.67.3",
    "vitaldb>=1.6.4",
]
